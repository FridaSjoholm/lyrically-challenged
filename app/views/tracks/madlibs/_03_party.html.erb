<%= form_tag("/search_for_party", method: "get", class: "form-search", remote: true) do %>

 <h1>Looking for a song for my
  <%= text_field_tag(:word, "") %>
  themed party

  <%= submit_tag("Search", class:"btn btn-send") %>
  </h1>
<% end %>
